{
  "id": "code_name_hsn_gst_price",
  "description": "Catalog entries with code, description, HSN/GST tails, and INR price",
  "columns": [
    {
      "role": "code",
      "required": true,
      "header_keywords": ["Cat No", "Catalog", "Cat", "Code"],
      "value_regex": "([A-Z0-9]{3,}[-/][A-Z0-9-]{2,}|[A-Z]{2,}\\d{3,}|[0-9]{4,}-[0-9]{2,}|[0-9]{6,})"
    },
    {
      "role": "name",
      "required": true,
      "header_keywords": ["Product Description", "Description", "Product", "Name"],
      "min_length": 5
    },
    {
      "role": "notes",
      "required": false,
      "header_keywords": ["HSN"],
      "value_regex": "\\b[0-9]{6,}\\b"
    },
    {
      "role": "notes",
      "required": false,
      "header_keywords": ["GST"],
      "value_regex": "\\b[0-9]{1,2}%\\b"
    },
    {
      "role": "price",
      "required": true,
      "header_keywords": ["INR", "Price", "LP", "List"],
      "value_regex": "\\d"
    }
  ],
  "validation": {
    "required_roles": ["code", "name", "price"],
    "optional_roles": ["pack"],
    "min_confidence": 0.45
  },
  "normalization": {
    "currency": {
      "default": "INR",
      "symbols": { "₹": "INR", "Rs": "INR" },
      "codes": { "INR": "INR" }
    },
    "units": {
      "pack": []
    }
  }
}
