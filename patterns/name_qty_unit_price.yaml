{
  "id": "name_qty_unit_price",
  "description": "Commodity style name + quantity/unit + price",
  "columns": [
    {
      "role": "name",
      "required": true,
      "header_keywords": ["Description", "Product", "Item"],
      "min_length": 6
    },
    {
      "role": "pack",
      "required": true,
      "header_keywords": ["Qty", "Unit", "Pack"],
      "value_regex": "\\b\\d+(?:\\.\\d+)?\\s?(KG|G|L|ML|TON|BAG|CTN|CARTON|CASE)\\b"
    },
    {
      "role": "price",
      "required": true,
      "header_keywords": ["Price", "Rate", "Amount"],
      "value_regex": "\\d"
    }
  ],
  "validation": {
    "required_roles": ["name", "pack", "price"],
    "min_confidence": 0.45
  }
}
